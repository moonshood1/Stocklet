      <div id="carouselExampleSlidesOnly" class="carousel slide" data-ride="carousel">
        <div class="carousel-inner">
          <div class="carousel-item active">
            <img
              class="d-block w-100"
              {% if product.leftPic1URL is null and product.leftPic1Name %}
                src="/uploads/images/product_left/{{ product.leftPic1Name }}"
              {% else %}
                src="{{ product.leftPic1URL }}"
              {% endif %}
              alt="First slide"
              width="100%"
            />
          </div>
          <div class="carousel-item">
            <img
              class="d-block w-100"
              {% if product.leftPic2URL is null and product.leftPic2Name %}
                src="/uploads/images/product_left/{{ product.leftPic2Name }}"
              {% else %}
                src="{{ product.leftPic2URL }}"
              {% endif %}
              alt="Second slide"
              width="100%"
            />
          </div>
          <div class="carousel-item">
            <img
              class="d-block w-100"
              {% if product.leftPic3URL is null and product.leftPic3Name %}
                src="/uploads/images/product_left/{{ product.leftPic3Name }}"
              {% else %}
                src="{{ product.leftPic3URL }}"
              {% endif %}
              alt="Third slide"
              width="100%"
            />
          </div>
        </div>
      </div>