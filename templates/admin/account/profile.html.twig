{% extends 'admin/base.html.twig' %}

{% block title %}Modification des données {% endblock %}

{% block body %}
        <div class="content-container">
            <div class="container-fluid">
                       <a href="{{ path('admin_account') }}" class="btn btn-primary" style="border-radius:50%">
                            <i class="fas fa-arrow-left fa-2x"></i>
                         </a>
                         <h1> Modifications des informations personnelles </h1>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                    <button type="submit" class="btn btn-primary" formnovalidate>Enregistrer les informations </button>
                {{ form_end(form) }}
            </div>
        </div>
{% endblock %}
